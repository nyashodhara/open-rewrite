package com.example.demo;

public class CamelCase {
    //convert variables to camel cases

    /*private String first_name;
    private int age_years;
    private boolean is_active;

    public CamelCase(String first_name, int age_years, boolean is_active) {
        this.first_name = first_name;
        this.age_years = age_years;
        this.is_active = is_active;
    }

    public void exampleMethod() {
        int num_items = 10;
        String user_name = "Alice";
        boolean is_valid = true;

        // Some processing code
        if (is_valid) {
            System.out.println(user_name + " has " + num_items + " items.");
        }
    }*/
}
